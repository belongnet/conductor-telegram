# TODOs

This file records intentionally deferred repository work. Package 1, making Telegram dependable from a phone,
shipped in v0.11.0. The packages below are ordered, not scheduled: releases are numbered when they ship.

## Open

- Package 2: deliver screenshots and files to Telegram from any workspace, including a one-call upload and upload tokens for app-created workspaces.
- Package 3a: keep the phone command menu in sync, accept inbound video, and treat listed skill hashtags as commands.
- Package 3b: `/cancel` and `/resume`.
- Package 3c: readable workspace names.
- Package 4 (only when asked): `/prs` snapshot card and a confirmed `/merge`.
