node_modules/
dist/
*.db
*.db-wal
*.db-shm
.env
.env.*
.gstack/

# Private workspace evidence, migration snapshots, and deployment configuration.
.context/
.conductor-telegram/

# Machine-local Conductor overrides. If these stay unignored, a dirty copy
# blocks `git checkout` of the next `conductor/telegram-*` workspace branch.
.conductor/settings.local.toml
.conductor/settings.local.json
